import { DataPropertyGetterPipe } from './data-property-getter.pipe';

describe('DataPropertyGetterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataPropertyGetterPipe();
    expect(pipe).toBeTruthy();
  });
});
